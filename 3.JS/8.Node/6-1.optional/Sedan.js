const Car = require('./Car');

class Sedan extends Car {
    // constructor(brand, model, color, trunkSize) {
    //     super(brand, model, color, trunkSize);
    // }
        // this.brand = brand;
        // this.model = model;
        // this.color = color;
        // this.trunkSize = trunkSize;
    // }
    // stop() {
    //     return `${brand} ${model}가 멈췄습니다.`;
    // }
}

module.exports = Sedan;